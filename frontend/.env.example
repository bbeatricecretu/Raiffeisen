# Frontend public URL used for referral links.
# Set this to your deployed Vercel domain or custom domain.
VITE_PUBLIC_APP_URL=https://your-app.vercel.app

# Backend API base URL used by the frontend.
# Must point to a publicly reachable backend.
VITE_API_BASE_URL=https://your-backend-domain.com/api
