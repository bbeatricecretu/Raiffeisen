TASK: Convert a Romanian user question into a structured JSON query for searching transactions.

OUTPUT: Return ONLY valid JSON that matches the provided JSON schema exactly.
- Do not include markdown.
- Do not include explanations.
- Do not add extra fields.

IMPORTANT:
- If the question contains time like "ieri", "săptămâna trecută", "luna trecută", output time_range as one of:
  "today", "yesterday", "last_week", "last_month", "all_time", "custom"
- If time_range = "custom", include custom_range with "from" and "to" in YYYY-MM-DD.
- If unsure, use "all_time".
- city/merchant/service_type can be null if not specified.
- sort must be "recent" or "oldest".
- limit must be an integer between 1 and 100.

INPUT (Romanian question):
{{QUERY_RO}}

TODAY (YYYY-MM-DD):
{{TODAY}}

SERVICE TYPE MAPPING RULES:
- If the question contains words like "mâncat", "restaurant", "prânz", "cină", set service_type to "Restaurant".
- If it contains "benzinărie", "carburant", set service_type to "Carburant".
- If it contains "asigurare", set service_type to "Asigurare".
- If unsure, set service_type to null.


Return ONLY JSON.
